# This is a Databricks asset bundle definition for genie_poc_sales_pipeline.
# See https://docs.databricks.com/dev-tools/bundles/index.html for documentation.
bundle:
  name: genie_poc_sales_pipeline
  uuid: a95d53ed-077c-42dc-b8e0-fbd26b4429bd

include:
  - ./resources/*.yml

# Variable declarations. These variables are assigned in the dev/prod targets below.
variables:
  warehouse_id:
    description: The warehouse to use
  catalog:
    description: The catalog to use
  schema:
    description: The schema to use

targets:
  dev:
    # The default target uses 'mode: development' to create a development copy.
    # - Deployed resources get prefixed with '[dev my_user_name]'
    # - Any job schedules and triggers are paused by default.
    # See also https://docs.databricks.com/dev-tools/bundles/deployment-modes.html.
    mode: development
    default: true
    workspace:
      host: https://dbc-202f4808-1754.cloud.databricks.com
    variables:
      warehouse_id: ae5e5dee32261b2f
      catalog: genie_poc
      schema: raw

  prod:
    mode: production
    workspace:
      host: https://dbc-202f4808-1754.cloud.databricks.com
      # We explicitly deploy to /Workspace/Users/skyler.myers@v4c.ai to make sure we only have a single copy.
      root_path: /Workspace/Users/skyler.myers@v4c.ai/.bundle/${bundle.name}/${bundle.target}
    variables:
      warehouse_id: ae5e5dee32261b2f
      catalog: genie_poc
      schema: raw
    permissions:
      - user_name: skyler.myers@v4c.ai
        level: CAN_MANAGE
